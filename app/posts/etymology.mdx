import  ForceGraph from "../../components/force-graph";

export const meta = {
    title: 'Etymologies',
    description: '',
    thumbnail: '',
    hidden: false,
    priority: 0,
}

export const nodes = [{id: 'a'}, {id: 'b'}];
export const edges = [{source: 'a', target:'b'}];

<ForceGraph nodes={nodes} edges={edges} graphId={'test'}/>